global
	log /dev/log	local0
	log /dev/log	local1 notice
	chroot /var/lib/haproxy
	stats socket /run/haproxy/admin.sock mode 660 level admin
	stats timeout 30s
	user haproxy
	group haproxy
	daemon

	# Default SSL material locations
	ca-base /etc/ssl/certs
	crt-base /etc/ssl/private

	# See: https://ssl-config.mozilla.org/#server=haproxy&server-version=2.0.3&config=intermediate
        ssl-default-bind-ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384
        ssl-default-bind-ciphersuites TLS_AES_128_GCM_SHA256:TLS_AES_256_GCM_SHA384:TLS_CHACHA20_POLY1305_SHA256
        ssl-default-bind-options ssl-min-ver TLSv1.2 no-tls-tickets

defaults
	log	global
	mode	http
	option	httplog
	option	dontlognull
        timeout connect 5000
        timeout client  50000
        timeout server  50000
	errorfile 400 /etc/haproxy/errors/400.http
	errorfile 403 /etc/haproxy/errors/403.http
	errorfile 408 /etc/haproxy/errors/408.http
	errorfile 500 /etc/haproxy/errors/500.http
	errorfile 502 /etc/haproxy/errors/502.http
	errorfile 503 /etc/haproxy/errors/503.http
	errorfile 504 /etc/haproxy/errors/504.http


frontend http
    bind *:80
    bind *:443 ssl crt /etc/haproxy/certs/
    acl letsencrypt_acme path_beg /.well-known/acme-challenge
    use_backend lego if letsencrypt_acme
    http-request redirect scheme https unless { ssl_fc } or letsencrypt_acme
	use_backend %[req.ssl_sni,lower] if { req.ssl_sni -m found }
	default_backend default_html


frontend prometheus
  bind 127.0.0.1:8405
  mode http
  http-request use-service prometheus-exporter if { path /metrics }
  no log


listen stats
    bind  *:9000 ssl crt /etc/haproxy/certs/ alpn h2,http1.1
    mode http
    stats enable
    stats uri /stats
    stats refresh 10s
    stats hide-version
    stats auth {{ haproxy_stats_admin_user }}:{{ haproxy_stats_admin_user_password }}

backend default_html
	mode http
	http-request return status 200 content-type "text/html" file /etc/haproxy/default.html

backend lego
    mode http
    server local_lego 127.0.0.1:8888


{% for service in haproxy_all_services %}
backend {{ service.name }}
    mode http
    server {{ service.name }} {{ service.IP }}:{{ service.Port }}

{% endfor %}
